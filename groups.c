#include <stdio.h>

#include "typing.h"
#include "groups.h"

/// @brief Read groups from file
/// @return 0 if no error, 1 if file read error
int get_groups() {
    FILE *fptr = fopen("groups.txt", "r");

}